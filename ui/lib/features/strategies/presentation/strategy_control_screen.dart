import 'package:flutter/material.dart';

class StrategyControlScreen extends StatelessWidget {
  const StrategyControlScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('StrategyControlScreen'));
  }
}
