import 'package:flutter/material.dart';

class RiskControlScreen extends StatelessWidget {
  const RiskControlScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('RiskControlScreen'));
  }
}
