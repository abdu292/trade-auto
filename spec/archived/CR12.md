بسم الله الرحمن الرحيم

السلام عليكم ورحمة الله وبركاته

Please treat this as an immediate live-engine correction note based on the latest Japan/India-session logs and charts.

============================================================
1) FIRST: LEDGER / DASHBOARD IS STILL WRONG
============================================================

The dashboard is still displaying scaled / incorrect balances.

It is showing values like:
- Cash: 112853.18 AED
- Gold: 229200.00 g

But the real physical ledger state is:
- Cash: 2237.42 AED
- Gold: 2292.00 g

This must be fixed immediately.

Hard rule:
- physical ledger values must be shown exactly as stored in SLIPS
- no x100 / x50 / x10 scaling
- MT5 account card must remain separate from physical ledger card

Required fix:
- audit the unit conversion and display mapping for cash and grams
- confirm the source field used by Capital Dashboard
- ensure all trading size calculations for micro mode use:
  cash = 2237.42 AED
  owned gold = 2292.00 g

============================================================
2) CORE LIVE ISSUE: BOTTOMPERMISSION IS TOO STRICT
============================================================

Latest logs show:

- waterfallRisk = LOW
- NoFail = True
- NoHazard = True
- SpreadOk = True
- H1Bullish = True
- M15Base became True(min=2) in the later log
- but decision still aborted because:
  BOTTOMPERMISSION_FALSE

This means the system is not rejecting because the setup is dangerous.
It is rejecting because the bottom-permission gate is too rigid.

That is now blocking valid India-session / continuation-rebuild rotations.

============================================================
3) WHAT THE CHARTS SUGGEST
============================================================

From the current H1 / M30 / M15 screenshots:

- H1 is bullish / constructive, not liquidation
- M30 shows rebound / rebuild from the lower area
- M15 shows structure improving and price holding above the local rebuild zone
- this is not a first-leg waterfall catch
- this is not a shock-news panic structure
- this looks closer to:
  RANGE_RELOAD / CONTINUATION_REBUILD
  than to LIQUIDATION or true EXHAUSTION failure

So the current gate is over-blocking.

============================================================
4) REQUIRED FIX — SPLIT BOTTOMPERMISSION INTO TWO MODES
============================================================

Please stop using one rigid bottom-permission rule for all bullish entries.

Create two separate legal paths:

------------------------------------------------------------
A) DEEP BOTTOM CATCH MODE
------------------------------------------------------------

This is the strict mode.

Use only when trying to buy near the true bottom / flush area.

Require:
- H1 sweep-reclaim OR strong flush-reclaim evidence
- M15 base stronger
- M5 compression / reclaim evidence
- strong bottom permission

This mode should stay strict.

------------------------------------------------------------
B) CONTINUATION REBUILD MODE
------------------------------------------------------------

This is the relaxed-but-safe mode.

Use when:
- H1 is bullish or recovering bullish
- NoFail = True
- NoWaterfall = True
- NoHazard = True
- SpreadOk = True
- M15Base = True(min=2) OR defended shelf clearly rebuilding
- price is no longer in liquidation mode
- path is not mid-air
- structure is a rebuild / range reload / continuation retest

In this mode:
- H1SweepReclaim must NOT be mandatory
- M5Compression must NOT be mandatory
- M5Align must be optional / confidence booster only
- BUY_LIMIT may be allowed from defended rebuild shelf
- BUY_STOP may be allowed only if true lid/compression later appears

This is the main fix required.

============================================================
5) NEW RULE FOR BOTTOMPERMISSION
============================================================

Please replace the current single hard-fail logic with this:

If:

- NoFail = True
- NoWaterfall = True
- NoHazard = True
- SpreadOk = True
- H1Bullish = True
- M15Base = True(min >= 2)

Then:

BOTTOMPERMISSION must not auto-fail.

Instead:
- classify setup type:

  if near true flush bottom:
    use DEEP_BOTTOM_CATCH rules

  else if bullish rebuild / range reload / continuation retest:
    use CONTINUATION_REBUILD rules

Only fail if BOTH are invalid.

============================================================
6) DO NOT USE M5 AS A UNIVERSAL VETO
============================================================

Current logs show the block often happens because:
- M5Compression = False
- M5Align = False

For BUY_LIMIT continuation-rebuild setups, this is too strict.

Required rule:
- For BUY_LIMIT on rebuild shelf:
  M5 confirmation is optional
  it may improve confidence / size
  but it must not be a universal veto

Keep M5 stricter only for:
- BUY_STOP continuation breakout
- deep flush catch requiring very precise stabilization
- high-volatility / high-caution windows

============================================================
7) ADD A NEW ENGINE STATE
============================================================

Please add:

bottomPermissionMode =
- DEEP_BOTTOM
- CONTINUATION_REBUILD
- NONE

And log it explicitly.

Also log:
- why DEEP_BOTTOM failed
- why CONTINUATION_REBUILD passed or failed

This will stop us from getting a vague single:
BOTTOMPERMISSION_FALSE
without knowing which valid path was blocked.

============================================================
8) CANDIDATE LIFECYCLE PATCH
============================================================

For this exact kind of setup, candidate lifecycle should behave like:

- FORMING when H1 bullish + M15 shelf appears
- CANDIDATE when M15Base=True(min=2)
- ARMED if NoFail + NoWaterfall + NoHazard + SpreadOk + not mid-air
- PENDING_PLANTED if TABLE can compile legal micro BUY_LIMIT

Do not wait for perfect bottom-catch confirmation if the structure has already transitioned into continuation-rebuild mode.

============================================================
9) MODE / REGIME FIX
============================================================

The log currently says:
mode: EXHAUSTION

This appears too pessimistic for the shown structure.

Please refine regime classification so that after:
- successful rebound
- shelf rebuilding
- H1 bullish context
- M15Base True
- NoFail / NoWaterfall / NoHazard

the regime can upgrade from:
EXHAUSTION
to one of:
- RANGE_RELOAD
- CONTINUATION_REBUILD
- BULLISH_RECOVERY

EXHAUSTION should not remain sticky when the structure has materially repaired.

============================================================
10) WHAT TABLE SHOULD BE ALLOWED TO DO HERE
============================================================

For this class of setup, TABLE should be allowed to compile a micro BUY_LIMIT if all these hold:

- H1Bullish = True
- M15Base = True(min=2)
- NoFail = True
- NoWaterfall = True
- NoHazard = True
- SpreadOk = True
- entry anchored near defended rebuild shelf
- not mid-air
- TP realistic to first magnet
- expiry short and session-bound

This is exactly the kind of safe micro rotation we do not want the engine to miss.

============================================================
11) KEEP THESE BLOCKS INTACT
============================================================

Do NOT weaken:
- FAIL block
- WATERFALL_RISK block
- hazard block
- spreadBlock
- mid-air ban
- capital / ledger law
- no market-buy law

This patch is not asking for recklessness.
It is asking the engine to stop treating every valid rebuild as if it were a dangerous bottom catch.

============================================================
12) IMMEDIATE IMPLEMENTATION TASKS
============================================================

Please do these next:

1. Fix dashboard ledger scaling immediately
   - cash = 2237.42 AED
   - gold = 2292.00 g

2. Split bottomPermission into:
   - DEEP_BOTTOM
   - CONTINUATION_REBUILD

3. Remove mandatory H1SweepReclaim for continuation-rebuild buys

4. Make M5 compression / M5 align optional for BUY_LIMIT rebuild entries

5. Add explicit bottomPermissionMode logging

6. Allow regime upgrade from EXHAUSTION to RANGE_RELOAD / CONTINUATION_REBUILD when structure repairs

7. Re-test this exact setup class in India-session conditions

============================================================
13) EXPECTED RESULT AFTER PATCH
============================================================

After this fix, the engine should:

- still block real waterfall catches
- still protect capital
- but stop rejecting valid bullish rebuild / range-reload setups
- arm earlier micro BUY_LIMITs on defended shelves
- improve safe rotation count
- reduce false NO_TRADE outcomes caused by over-strict bottom rules

============================================================
14) Fix session detection
============================================================

جزاك الله خيراً
والحمد لله