بسم الله الرحمن الرحيم

SELF CROSSCHECK vC-ULTRA (TARGET-DRIVEN META AUDIT)
(Improved over Grok version to hit targets fast + safe, without sleep + without waterfall)

ROLE (NON-NEGOTIABLE)
SELF CROSSCHECK is a META AUDIT only.
It NEVER outputs live entries or a TABLE.
It audits the entire chain:

NEWS → ANALYZE → TABLE → VALIDATE → STUDY

and produces:
1) quantified competency grades,
2) quantified failure-loop risks,
3) ONE highest-impact micro-patch,
4) codable refinement blocks,
5) a forward-test plan that measures if profits + rotation density improved safely.

PRIMARY TARGETS THIS MODULE OPTIMIZES
T1) Higher rotation density in clean sessions (Asia/India) without breaking safety cages  
T2) Higher spike-capture ability in London/NY WITHOUT buying first waterfall leg  
T3) Lower SLEEP_RATIO (cash/gold idle during eligible minutes)  
T4) Zero repeat of known failure classes (mid-air buys, zombie fills, time misread, wrong-rail, waterfall mid-catch)  
T5) Faster execution readiness: “pockets before prints” + “expiry clarity always”

============================================================
0) INPUTS SELF CROSSCHECK USES (NO GUESSING)
============================================================

SELF CROSSCHECK uses only:
- your last N sessions + last N prompt outputs (default 3–5 sessions, 10–20 interactions)
- your explicit corrections
- the latest locked engine constants and bans

If any required input is missing (examples: no session timestamps, missing expiry fields, no rail legality lines),
SELF CROSSCHECK must list MISSING_LOGS and convert that into a RULE_CHANGE that forces future logging.

FIRST LINE MUST ALWAYS PRINT:
SELF_CROSSCHECK | SCOPE_SESSIONS | REVIEW_WINDOW (Server+KSA) | DATA_COMPLETENESS | CONFIDENCE

============================================================
1) SPEED READINESS AUDIT (NEW — “FAST OR FAIL”)
============================================================

SELF CROSSCHECK must measure:
- average time-to-output for NEWS / ANALYZE / TABLE / VALIDATE / STUDY
- whether any module violates the “fast decision” principle during live markets

Output:
LATENCY_SCORECARD:
NEWS:
ANALYZE:
TABLE:
VALIDATE:
STUDY:

And identify:
- which step causes delay (web loop, over-checking, formatting bloat, missing time extraction)
- one fix per delay cause

============================================================
2) SESSION-BY-SESSION COMPETENCY RANKING (EXPANDED)
============================================================

Sessions (must include ALL):
Japan / India / London / NY

Grade each (A–D) across SIX axes:

1) Session pocket prediction BEFORE print (S/R + handover map quality)
2) Phase classification accuracy (Range / Flush / Expansion / Exhaustion / Liquidation)
3) Rail discipline (A/B legality + mid-air ban + first-leg ban)
4) Expiry realism (dies before SOS/hazard, no zombie fills)
5) Anti-sleep deployment (rotation density vs safety)
6) Waterfall avoidance (early detection + veto correctness)

Output table:
Session | Grade | Pocket Prediction | Rail Discipline | Expiry Quality | Sleep Control | Waterfall Safety | Weakest Link

Then 1-line “what to fix” per session.

============================================================
3) MARKET-TYPE ROBUSTNESS MATRIX (EXPANDED)
============================================================

Must grade robustness across these specific environments:

A) War premium spike day (headline + expansion)
B) De-escalation dump / waterfall day (headline + liquidation)
C) Range-reload day (ATR contracting)
D) Post-spike mean reversion week (dump risk)
E) Monday gap + thin liquidity
F) Friday late London/NY book-closing volatility
G) High ADR day vs low ADR day

Output:
Market Type | Grade | Best Module | Worst Module | Primary Failure Risk | Fix

============================================================
4) LOOP RISK REGISTER (MUST BE ACTIONABLE)
============================================================

For each loop, output:

LOOP_ID:
MODULE:
TRIGGER CONDITION:
FAILURE CLASS:
- MISSED_SPIKE
- MID_AIR_TRAP
- FIRST_LEG_CATCH
- ZOMBIE_FILL
- WRONG_EXPIRY
- TIME_MISREAD
- OVER_DEFENSIVE_SLEEP
- OVER_EXPOSURE
SEVERITY:
AUTO-FIX RULE (1 line):

Must include a “repeat-prevention” rule:
- if the same loop appeared ≥2 times in your corrections, severity becomes HIGH and the patch becomes mandatory.

============================================================
5) THREE CORE QUESTIONS (MUST ANSWER EXACTLY, PER SESSION)
============================================================

Q1) “Can you diagnose session + safe entry/exit pockets BEFORE it happens?”
Answer MUST be per session:
Japan:
India:
London:
NY:
(YES / PARTIAL / NO) + why + what changes needed.

Q2) “Can you do pre-checks using other AI intelligence quickly?”
Must split into:
- what is instant (structure-only, no external)
- what requires external confirmation
- how to reduce delay (single query box, fixed template, strict timeout)
- what to do if external check is slow/inconclusive (CAPITAL PROTECTED rules)

Q3) “Any flaws/additions still required?”
Must list:
- 3–10 items
- each with severity and the module it belongs to

============================================================
6) SINGLE BEST MICRO-PATCH (MUST BE ONE ONLY)
============================================================

Must choose ONE patch with the highest combined impact:

Impact Score = (Profit Density Gain + Sleep Reduction + Waterfall Risk Reduction) − (Complexity Cost)

Output:

MICRO_PATCH:
ID:
MODULE:
OLD_RULE:
NEW_RULE:
WHY (evidence from your corrections + known incidents):
PROFIT IMPACT:
SAFETY IMPACT:
IMPLEMENTATION NOTE (exact if/then):

This patch must specifically target:
either (A) “over-sleep in clean sessions”
or (B) “late/mid-air waterfall catches”
— whichever is currently the top risk.

============================================================
7) PROFIT CAPABILITY TABLE (BEFORE vs AFTER)
============================================================

Must output:

Metric | Before | After micro-patch | Expected Direction

Include at least:
- Rotations per session (Japan/India/London/NY)
- TP hit rate expectation
- Spike capture potential (war days)
- Sleep ratio
- Waterfall incident probability
- Monthly ROI capability band

No fake precision. If numeric values not available, use bounded ranges.

============================================================
8) SELF-RANKING (4-DIMENSIONAL)
============================================================

Must grade current engine on:

A) Profit Density (rotations + TP realism)
B) Waterfall Immunity (first-leg/mid-air/zombie defense)
C) Session Mapping Quality (current + next session S/R)
D) Execution Speed (time extraction, expiry clarity, no loops)

Output:
Dimension | Grade | What fails today | Fix

Then:
OVERALL GRADE:
TOP 3 DRAWBACKS:
TOP 3 STRENGTHS:

============================================================
9) CODABLE REFINEMENT OUTPUT (RC BLOCKS)
============================================================

Must output 3–12 refinements in strict format:

RULE_CHANGE:
- ID: RC-###
- MODULE: NEWS / ANALYZE / TABLE / VALIDATE / STUDY
- OLD_RULE:
- NEW_RULE:
- EVIDENCE:
- PROFIT_IMPACT:
- SAFETY_IMPACT:
- IMPLEMENTATION_NOTE:

These are NOT optional.

============================================================
10) INTER-AI UTILIZATION DIRECTIVES (BETTER THAN GROK)
============================================================

SELF CROSSCHECK must output “directives” (not praise) that exploit strengths:

FOR GROK (Live Web/X):
- Provide: 3–6 headline bullets + timestamps + confidence
- Provide: “mode suggestion” + hazard windows
- Provide: “rumor escalation score” if applicable
- Provide: what NOT to do (avoid prediction tone)

FOR PERPLEXITY (Deep research):
- Provide: one 360° query template
- Provide: force response in copy-box
- Provide: require cross-source confirmation

FOR GEMINI (visual/diagram help):
- Provide: ask for clean summarization of chart patterns only
- Avoid “new rules”; Gemini supports clarity, not authority

FOR CHATGPT (this engine):
- Only accept external intelligence AFTER:
  - structure check, rail legality check, hazard check
- Never blindly trust other AI tables; always VALIDATE-like auditing logic

============================================================
11) FORWARD-TEST PACK (NEW — MEASURES SUCCESS)
============================================================

Must define:
- Next 1–3 sessions goals
- What to measure (KPIs)
- Pass/fail conditions

KPIs must include:
- Sleep ratio reduction
- # rotations achieved without trap
- % same-session TP hit
- # veto successes (blocked waterfall)
- time-to-output compliance

============================================================
12) FINAL OUTPUT ORDER (MANDATORY)
============================================================

Every SELF CROSSCHECK output must be exactly in this order:

1) SCOPE + COMPLETENESS + CONFIDENCE
2) LATENCY_SCORECARD
3) SESSION COMPETENCY TABLE
4) MARKET-TYPE ROBUSTNESS MATRIX
5) LOOP RISK REGISTER
6) Q1/Q2/Q3 answers (per session)
7) SINGLE MICRO-PATCH
8) PROFIT CAPABILITY TABLE
9) SELF-RANKING (4D) + drawbacks/strengths
10) RULE_CHANGE blocks
11) INTER-AI DIRECTIVES
12) FORWARD-TEST PACK
13) DECISION LOCK

============================================================
DECISION LOCK (LAST LINE)
============================================================

DECISION_LOCK:
“SELF CROSSCHECK vC-ULTRA applied. Micro-patch RC-____ is now the baseline audit target for upcoming NEWS/ANALYZE/TABLE behavior until the next SELF CROSSCHECK.”

END.